name: Content Quality and Discoverability Audit

on:
  schedule:
    - cron: '0 6 * * 6'
  workflow_dispatch:
    inputs:
      skip_url_check:
        description: 'Skip live ai-news URL checks'
        required: false
        default: false
        type: boolean

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt
          pip install -e ./scripts

      - name: Content quality audit
        run: |
          ARGS=()
          if [ "${{ github.event.inputs.skip_url_check }}" = "true" ]; then
            ARGS+=(--skip-url-check)
          fi
          python scripts/audit_content_quality.py "${ARGS[@]}"

      - name: Discoverability audit (posts only)
        run: python scripts/audit_discoverability.py --skip-pagefind

  site-discoverability:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3'
          bundler-cache: true

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/requirements.txt
          pip install -e ./scripts

      - name: Build site and Pagefind index
        run: |
          bundle exec jekyll build --baseurl ""
          npx --yes pagefind@1.3.0 --site _site

      - name: Discoverability audit (Pagefind)
        run: python scripts/audit_discoverability.py --site-dir _site

  notify-failure:
    runs-on: ubuntu-latest
    needs: [audit, site-discoverability]
    if: failure()
    steps:
      - name: Notify Slack
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          if [ -z "$SLACK_WEBHOOK_URL" ]; then
            echo "SLACK_WEBHOOK_URL not configured, skipping Slack notification."
            exit 0
          fi
          RUN_URL="${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          curl -sS -X POST -H 'Content-type: application/json' \
            --data "{\"text\":\"⚠️ Content quality / discoverability audit failed\n${RUN_URL}\"}" \
            "$SLACK_WEBHOOK_URL"